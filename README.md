# Content AI — Frontend (frontside)

## Setup
```bash
npm install
npm run dev
```

## Production build
```bash
npm run build
npm run preview
```

Set `VITE_API_URL` or use the Vite proxy to your API (`/api` → backend).
